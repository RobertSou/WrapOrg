doctype html
html
    include partials/header.pug
    body
        #backBtn
            a(href="/")
                img(src="/images/back.svg", alt="Back button")
        main#mOngRegistroWrapper
            div(id="boxOngRegistro")
                header.HeaderDoador
                    h1 Bem vindo ONG
                    h4 Sua ONG precisa de uma ajuda para arrecadar doações e achar doadores? nós podemos ajudar.
                div#form
                    h2 Registro
                    form.formDoador(action="/ong/registro", method="post")
                        include partials/messages.pug
                        input#nome(
                            type="text"
                            name="name"
                            placeholder="Digite o nome da ONG"
                            value=typeof name != 'undefined' ? name : ''
                        )
                        input#cnpj(
                            type="text"
                            name="cnpj"
                            placeholder="Digite o CNPJ da ONG"
                            value=typeof cnpj != 'undefined' ? cnpj : ''
                        )
                        input#EmailRegister(
                            type="email"
                            name="email"
                            placeholder="Digite o email da ONG"
                            value=typeof email != 'undefined' ? email : ''
                        )
                        input#PasswordRegister(
                            type="password"
                            name="password"
                            placeholder="Digite uma senha"
                            value=typeof password != 'undefined' ? password : ''
                        )
                        input#RPasswordRegister(
                            type="password"
                            name="rpassword"
                            placeholder="Digite a senha novamente"
                            value=typeof rpassword != 'undefined' ? rpassword : ''
                        )
                        input(type="submit" value="REGISTRAR")
                    p(class="linkToOtherPage") Já tem uma conta?
                        a(href="/ong/login") Fazer login.
    script(src="/js/main.js")